import React from 'react';
import Layout from '../../components/Layout';

const Search = () => {
    return (
        <Layout>
            Hello from Search
        </Layout>
    );
};

export default Search;